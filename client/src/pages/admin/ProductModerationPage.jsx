import Card from '../../components/common/Card';

export default function ProductModerationPage() {
  return (
    <Card>
      <p className="c_eyebrow">Product moderation</p>
      <h2>Moderation-ready inventory surface</h2>
      <p>Wire this page to admin product listing endpoints when moderation workflows are implemented. No fake inventory is rendered here anymore.</p>
    </Card>
  );
}
