import Card from '../../components/common/Card';

export default function AdminCreatorDetailPage() {
  return (
    <div className="c_page-stack">
      <Card>
        <p className="c_eyebrow">Creator detail</p>
        <h2>Creator inspection view</h2>
        <p>This surface is scaffolded for production admin workflows, but it now expects a real creator id-driven drilldown instead of local mock records.</p>
      </Card>
    </div>
  );
}
